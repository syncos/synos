Kernel drivers
==============

Because of micro-kernel origented approach synos has taken, the drivers/kernel directory is where most of the magic happens in the kernel. 
The drivers here are statically linked to the kernel and consists of for example a process scheduler and a module manager.